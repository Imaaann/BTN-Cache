{
  "name": "cache-example-express",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "redis:start": "docker-compose up -d redis",
    "redis:stop": "docker-compose down",
    "redis:logs": "docker-compose logs -f redis",
    "server": "ts-node src/server.ts",
    "dev": "npm run redis:start && npm run server"
  },
  "dependencies": {
    "@btn/cache": "*",
    "express": "^5.2.1",
    "ioredis": "^5.8.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "ts-node": "^10.9.2",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  }
}
